{
  "name": "bot-zero",
  "version": "0.0.1",
  "private": true,
  "author": "Wehkamp (kbakker@wehkamp.nl)",
  "description": "An 'empty' bot that can be the start for your own bot.",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^5.0.1",
    "hubot": "^2.19.0",
    "hubot-command-mapper": "^1.2.8",
    "hubot-help": "^0.2.2",
    "hubot-slack": "^4.4.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/mocha": "^5.2.0",
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "hubot-pretend": "^1.2.0"
  },
  "engines": {
    "node": "0.10.x"
  },
  "scripts": {
    "start": "hubot --adapter slack",
    "test": "mocha tests/**/*.spec.js --timeout 10000 --exit"
  }
}
